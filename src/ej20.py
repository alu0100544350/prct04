#!/usr/bin/python
#!encoding: UTF-8


x=-b/a

a=float(raw_input('valor de a: '))
b=float(raw_input('valor de b: '))

print'solucion: ', x

#El programa se supone que lee dos números y realiza el cociente entre ellos. Lo que esra mas es que hace la operación antes de saber el valor de las variables, es decir, antes de pediras.